# Breaking changes, acknowledged

<!-- acknowledgements -->

## tool `w` was removed
- migration: none
  this continuation line is not part of the migration field — the format
  has no continuation syntax, so it must be silently ignored, not appended
- tells the user: it is gone.
